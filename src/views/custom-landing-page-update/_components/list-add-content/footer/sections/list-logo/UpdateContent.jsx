import React from "react";

const UpdateContent = ({
  currentSection,
  currentContent,
  setPreviewSection,
  isEditingContent,
}) => {
  return <div>UpdateContent</div>;
};

export default UpdateContent;
